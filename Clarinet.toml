[project]
name = "delegate-scan"
authors = ["Your Name"]
description = "A decentralized platform for tracking and verifying delegate contributions"
telemetry = false
requirements = []

[contracts.delegate-tracking]
path = "contracts/delegate-tracking.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
